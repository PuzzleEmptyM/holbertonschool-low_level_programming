#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers and returns the result.
 * @a: input integer 1
 * @b: input integer 2
 * Return: int.
 */

int add(int a, int b)
{
	int c;

	c = (a + b);
	return (c);
}
