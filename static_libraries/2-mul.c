#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers.
 * @a: first input value
 * @b: second input value
 * Return: int.
 */

int mul(int a, int b)
{
	return (a * b);
}
